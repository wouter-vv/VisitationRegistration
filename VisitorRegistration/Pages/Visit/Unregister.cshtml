@page
@model VisitorRegistration.UnregisterModel

@{
    ViewData["Title"] = "Unregister";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Unregister</h1>

<h4>Visitation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">

            <div class="form-group">
                <label asp-for="Visitation.Person.Id" class="control-label">Wie bent u: </label>
                <select asp-for="Visitation.Person.Id" class="form-control" asp-items="ViewBag.PersonsLoggedIn"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

